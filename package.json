{
  "name": "playwright-mcp",
  "version": "1.0.0",
  "description": "step 1- install the github copilot from the vscode extention step 2- install playwright (npm init playwright@latest) and after that install the playwright MCP server",
  "main": "index.js",
  "scripts": {},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/node": "^24.0.10"
  }
}
